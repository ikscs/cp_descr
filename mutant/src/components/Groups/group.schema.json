{
  "title": "group.title",
  "type": "object",
  "required": [
    "name"
  ],
  "properties": {
    "group_id": {
      "type": "integer",
      "title": "group.id",
      "readOnly": true
    },
    "name": {
      "type": "string",
      "title": "group.name",
      "minLength": 1
    },
    "point_id": {
      "type": ["integer", "null"],
      "title": "group.pointID",
      "enum": [1,2,3]
    }
  }
}
